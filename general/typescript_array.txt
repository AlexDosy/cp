length:
    arr.length
max :
    Math.max(arg1, arg2)
sort:
    arr.sort((a,b)=> a-b). //ascending
    arr.sort((a,b)=> b-a). //descending

this is how array can be filled and initialized
new Array(n).fill(1)

//ts set implementation and filter function
    const set2 = new Set(nums2)

    const diff1= Array.from(set1).filter(num=>!set2.has(num))


//const values1= [...freq1.values()].sort((a,b)=>a-b)
