here we use two pointers
we see the leftmax and rightmax of every single position
from the miň subtract all the input values, and ignore all the negative values
added together we get the required value